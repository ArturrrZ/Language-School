export type ApplyType = {
  display: boolean;
  title: string;
  text: string;
};

export type TeacherType = {
  id: number;
  name: string;
  experience: string;
  photo?: string;
  message: string;
}